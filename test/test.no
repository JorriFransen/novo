
// #import "another.no"

Vec :: struct {
    x: s64;
    y: s64;
}

AABB :: struct {
    pos: Vec;
    size: Vec;
}

main :: () -> s64 {
    // return 72623859790382856; // 0x102030405060708
    // return 1 + 2;
    // return add(11, 22);
    // return add(11, 22) + add(1, 2);

    p : Vec;
    // p.x = 11;
    p.y = 22;

    // r : AABB;
    // r.pos = p;
    // p.x = 22;
    // p.y = 11;
    // r.size = p;

    return vec_y(p);
    // return add(add(r.pos.x, r.pos.y), add(r.size.x, r.size.y));
}

vec_y :: (v: Vec) -> s64 {
    return v.y;
}

add :: (a: s64, b: s64) -> s64 {
    // result : s64;
    // result = a + b;
    // return result;
    return a + b;
}
