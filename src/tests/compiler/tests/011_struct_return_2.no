
Vec :: struct {
    x: s64;
    y: s64;
}

vec :: (x: s64, y: s64) -> Vec {
    result: Vec;
    result.x = x;
    result.y = y;
    return result;
}

main :: () -> s64 {

    p := vec(11, 22);

    return p.x + p.y;
}

