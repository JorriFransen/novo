
Vec :: struct {
    x: s64;
    y: s64;
}

AABB :: struct {
    pos: Vec;
    size: Vec;
}

main :: () -> s64 {

    r : AABB;

    p : Vec;
    p.x = 40;
    p.y = 2;

    r.pos = p;
    r.size.x = p.y;
    r.size.y = p.x;

    return r.pos.x + r.pos.y + r.size.x + r.size.y;
}
